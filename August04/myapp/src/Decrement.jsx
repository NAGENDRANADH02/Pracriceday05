import React from 'react'

const Decrement = () => {

    return (
        <div>
            <button onClick={() => { setCount(count - 1) }}> Decrement</button>

        </div>
    )
}

export default Decrement
